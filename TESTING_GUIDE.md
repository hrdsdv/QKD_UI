# 📋 Руководство по тестированию QKD системы

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
cd C:\Users\Vaan\Desktop\qkd_encryption_system
pip install -r requirements.txt
```

### 2. Инициализация баз данных

Базы данных уже инициализированы! Если нужно пересоздать:

```powershell
python init_user_1_db.py
python init_user_2_db.py
```

### 3. Запуск серверов

**Терминал 1 - Сервер А (Абонент А):**
```powershell
cd server_user_1
python app.py
```

**Терминал 2 - Сервер Б (Абонент Б):**
```powershell
cd server_user_2
python app.py
```

### 4. Открыть браузеры

- **Абонент А:** http://localhost:5000
- **Абонент Б:** http://localhost:5001

---

## 👥 Учётные записи

| Пользователь | Логин | Пароль | Сервер |
|--------------|-------|--------|--------|
| Абонент А | `UserA` | `password123` | localhost:5000 |
| Абонент Б | `UserB` | `password123` | localhost:5001 |
| Администратор | `Admin` | `12345678qkd` | Оба сервера |

---

## 🧪 Сценарии тестирования

### Сценарий 1: Режим тестирования (генерация ключей)

1. **Войдите как UserA** на http://localhost:5000
2. **Нажмите "Режим тестирования"** (кнопка слева от поля ввода текста)
3. **Введите QBER:** например, `4` (процент ошибок)
4. **Нажмите "Генерация тестовой последовательности"**
5. **Проверьте метрики:**
   - Подключение к QKD-устройству: 1.1-3 сек
   - Генерация "сырого" ключа: 100%
   - Сверка базисов: QBER 4%
   - Восстановление ключа: % и время в мс
   - Шифрование/дешифрование: время в сек
   - Уничтожение ключа: "Использован и уничтожен"

### Сценарий 2: Выбор и восстановление ключа

1. **Нажмите "Выбор сырого ключа"**
2. **Увидите таблицу** с тестовыми последовательностями (помечены как "тест")
3. **Выберите последовательность** (нажмите "Выбрать")
4. **Проверьте:**
   - Этап "Просеивание ключа" загорелся зелёным
   - Отображается "Количество несовпадений = N"
   - QBER отображается в шапке страницы
5. **Нажмите "Восстановить"** у выбранной последовательности
6. **Проверьте:**
   - Этап "Восстановление ключа" загорелся зелёным
   - Появилось сообщение "Ключ успешно восстановлен!"

### Сценарий 3: Шифрование и отправка сообщения

**На сервере А (UserA):**

1. **Нажмите "Выбрать ключ"**
2. **Выберите восстановленный ключ** (например, `key_testA_20251127001659`)
3. **Введите текст** в поле "Введите текст для шифрования..."
   - Например: `Привет, Абонент Б! Это секретное сообщение.`
4. **Нажмите "Зашифровать и отправить"**
5. **Проверьте сообщение:**
   - "Сообщение зашифровано и отправлено!"
   - Время шифрования в мс
   - Отправлено получателю: Да

### Сценарий 4: Получение и дешифрование сообщения

**На сервере Б (UserB):**

1. **Перейдите на вкладку "Главная"** или обновите страницу
2. **В разделе "Входящие"** увидите зашифрованное сообщение
3. **Нажмите "Дешифровать"**
4. **Проверьте:**
   - Расшифрованный текст отображается
   - Время дешифрования в мс
   - Хэш: ✓ Верифицирован
   - Ключ: Уничтожен

### Сценарий 5: Синхронизация между абонентами

1. **На сервере А** выберите последовательность
2. **На сервере Б** откройте "Выбор сырого ключа"
3. **Проверьте:** та же последовательность помечена как "Выбрана A_UserA"
4. **На сервере Б** выберите ту же последовательность
5. **На обоих серверах** должен отображаться одинаковый QBER

### Сценарий 6: Проверка порога QBER (> 11%)

1. **Войдите в режим тестирования**
2. **Введите QBER:** `15` (больше 11%)
3. **Нажмите "Генерация тестовой последовательности"**
4. **Проверьте:**
   - Сверка базисов: "Отказ в обслуживании, ключевая последовательность скомпрометирована"
   - Остальные метрики пустые или с прочерками

---

## 🔧 Тестирование на двух разных ПК

### Подготовка:

1. **Узнайте IP-адреса:**
   ```powershell
   ipconfig
   ```
   Например: ПК1 = `192.168.1.100`, ПК2 = `192.168.1.101`

2. **Отредактируйте `config.py` на ПК1:**
   ```python
   REMOTE_SERVER_B = 'http://192.168.1.101:5001'
   ```

3. **Отредактируйте `config.py` на ПК2:**
   ```python
   REMOTE_SERVER_A = 'http://192.168.1.100:5000'
   ```

4. **Разрешите порты в брандмауэре** (5000 и 5001)

5. **Запустите серверы:**
   - ПК1: `cd server_user_1 && python app.py`
   - ПК2: `cd server_user_2 && python app.py`

6. **Откройте браузеры:**
   - ПК1: http://localhost:5000
   - ПК2: http://localhost:5001

---

## 🔐 Включение TLS (опционально)

### 1. Генерация сертификатов:

```powershell
python ssl/generate_certs.py
```

### 2. Включение TLS в config.py:

```python
USE_TLS = True
```

### 3. Обновление адресов:

```python
REMOTE_SERVER_A = 'https://localhost:5000'
REMOTE_SERVER_B = 'https://localhost:5001'
```

### 4. Перезапуск серверов

---

## 📊 Проверка работы модулей

### Модуль взаимодействия с QKD
- ✅ Генерация 256-битных последовательностей
- ✅ Сохранение в таблицу raw_data
- ✅ Отображение последних 8 бит с базисами

### Модуль постобработки (BB84)
- ✅ Сверка базисов между абонентами A и B
- ✅ Расчёт QBER
- ✅ Формирование просеянного ключа
- ✅ Проверка порога 11%

### Модуль восстановления (Рид-Соломон)
- ✅ Кодирование/декодирование RS
- ✅ Проверка хэша ГОСТ Р 34.11-2018 (Стрибог)
- ✅ Метрики: время, % восстановления, ошибки

### Модуль управления ключами
- ✅ Защищённое хранилище (AES-256-GCM)
- ✅ Политика одноразового использования
- ✅ Безвозвратное уничтожение
- ✅ Журнал аудита

### Модуль шифрования/дешифрования
- ✅ ГОСТ Р 34.12-2018 (Кузнечик) в режиме CTR
- ✅ Шифрование текстовых сообщений
- ✅ Проверка целостности (хэш)
- ✅ Отправка между абонентами

---

## 🐛 Устранение неполадок

### Ошибка: "Cannot find reference 'SERVER_A_HOST' in 'config.py'"
**Решение:** Это предупреждение IDE, не влияет на работу. Код работает корректно.

### Ошибка: "no such column: sifted_key"
**Решение:** Пересоздайте базы данных:
```powershell
python init_user_1_db.py
python init_user_2_db.py
```

### Ошибка: "UNIQUE constraint failed: keys.key_id"
**Решение:** Уже исправлено! Теперь используется `UPDATE` для существующих ключей.

### Ошибка: "Connection refused" при отправке сообщения
**Решение:** Убедитесь, что оба сервера запущены и доступны.

### Сервер автоматически залогинен
**Решение:** Очистите cookies браузера или используйте режим инкогнито.

---

## 📝 Журналы и логи

### Файловые логи:
- `qkd_system.log` — все события системы

### Логи в БД:
- Таблица `logs` — все операции с модулями
- Просмотр: Вкладка "Журнал действий" (для администратора)

---

## ✅ Чек-лист тестирования

- [ ] Оба сервера запускаются без ошибок
- [ ] Вход под разными пользователями (UserA, UserB)
- [ ] Генерация тестовой последовательности с заданным QBER
- [ ] Последовательность появляется в таблице "Сырые данные"
- [ ] Выбор последовательности синхронизируется между серверами
- [ ] Просеивание ключа отображает количество несовпадений
- [ ] QBER отображается в шапке страницы
- [ ] Восстановление ключа с проверкой хэша
- [ ] Ключ появляется в списке доступных ключей
- [ ] Шифрование сообщения с ГОСТ Р 34.12-2018
- [ ] Отправка зашифрованного сообщения на другой сервер
- [ ] Дешифрование сообщения получателем
- [ ] Уничтожение ключа после использования
- [ ] QBER > 12% приводит к отказу в обслуживании
- [ ] Все операции логируются в БД и файл

---

## 🎯 Ожидаемые результаты

После полного цикла тестирования:
1. Сообщение зашифровано ГОСТ Р 34.12-2018 (Кузнечик)
2. Сообщение передано между абонентами
3. Сообщение успешно дешифровано
4. Хэш верифицирован (ГОСТ Р 34.11-2018 Стрибог)
5. Ключ безвозвратно уничтожен
6. Все операции залогированы

