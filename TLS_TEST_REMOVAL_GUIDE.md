# 🗑️ Инструкция по удалению тестового кода TLS

## ⚠️ Важно!
Тестовый код для проверки TLS-канала можно безопасно удалить после завершения тестирования. Он не влияет на основную логику системы.

---

## 📋 Что нужно удалить:

### 1. Backend - server_user_1/app.py

**Удалить endpoints (строки ~750-850):**

```python
@app.route('/test_tls_send', methods=['POST'])
def test_tls_send():
    # ... весь метод ...

@app.route('/test_tls_receive', methods=['POST'])
def test_tls_receive():
    # ... весь метод ...

@app.route('/test_tls_get_messages', methods=['GET'])
def test_tls_get_messages():
    # ... весь метод ...

@app.route('/test_tls_clear', methods=['POST'])
def test_tls_clear():
    # ... весь метод ...
```

**Поиск:** `# ТЕСТОВЫЙ ENDPOINT` или `test_tls_`

---

### 2. Backend - server_user_2/app.py

**Удалить те же 4 endpoint'а:**

```python
@app.route('/test_tls_send', methods=['POST'])
@app.route('/test_tls_receive', methods=['POST'])
@app.route('/test_tls_get_messages', methods=['GET'])
@app.route('/test_tls_clear', methods=['POST'])
```

---

### 3. Frontend - server_user_1/templates/index.html

**Удалить HTML блок (найти по комментарию):**

```html
<!-- ТЕСТОВОЕ МОДАЛЬНОЕ ОКНО: Проверка TLS-канала -->
<!-- Можно удалить этот блок после тестирования без последствий -->
<div id="tls-test-modal" class="modal">
  ...
</div>

<!-- Кнопка для открытия TLS-теста (можно удалить) -->
<button id="open-tls-test" class="fixed bottom-4 right-4 ...">
  🔒 TLS
</button>
```

**Удалить JavaScript блок (найти по комментарию):**

```javascript
// ============================================
// ТЕСТОВЫЙ КОД: Проверка TLS-канала
// Можно удалить весь этот блок после тестирования
// ============================================

// ... весь код между комментариями ...

// ============================================
// КОНЕЦ ТЕСТОВОГО КОДА
// ============================================
```

---

### 4. Frontend - server_user_2/templates/index.html

**Удалить те же блоки HTML и JavaScript**

---

## 🔍 Как найти тестовый код:

### Метод 1: Поиск по комментариям
```bash
# В PowerShell
Select-String -Path "server_user_1/app.py" -Pattern "ТЕСТОВЫЙ ENDPOINT"
Select-String -Path "server_user_1/templates/index.html" -Pattern "ТЕСТОВЫЙ КОД"
```

### Метод 2: Поиск по названиям функций
```bash
Select-String -Path "*.py" -Pattern "test_tls_"
```

### Метод 3: Поиск по ID элементов
```bash
Select-String -Path "*.html" -Pattern "tls-test-modal"
```

---

## ✅ Проверка после удаления:

1. **Запустите серверы:**
   ```powershell
   cd server_user_1
   python app.py
   ```

2. **Проверьте основную функциональность:**
   - ✅ Генерация ключей работает
   - ✅ Восстановление ключей работает
   - ✅ Шифрование/дешифрование работает
   - ✅ Отправка сообщений работает
   - ✅ Входящие сообщения отображаются

3. **Убедитесь, что нет ошибок:**
   - Проверьте консоль браузера (F12)
   - Проверьте логи сервера
   - Проверьте `qkd_system.log`

---

## 📊 Что останется после удаления:

**Основная функциональность (не трогать!):**
- ✅ `/encrypt_message` - шифрование сообщений
- ✅ `/decrypt_message` - дешифрование сообщений
- ✅ `/api/receive_encrypted_message` - приём сообщений
- ✅ `/api/get_incoming_messages` - получение входящих
- ✅ `/recover_key` - восстановление ключей
- ✅ `/generate_test_sequence` - генерация QKD последовательностей

**UI компоненты (не трогать!):**
- ✅ Кнопка "Режим тестирования" (QKD)
- ✅ Кнопка "Выбор сырого ключа"
- ✅ Кнопка "Выбор восстановленного ключа"
- ✅ Раздел "Входящие"
- ✅ Вкладка "База данных" (для администратора)

---

## 💡 Когда удалять:

**Удаляйте тестовый код TLS когда:**
- ✅ Убедились, что TLS работает корректно
- ✅ Протестировали отправку/приём сообщений
- ✅ Проверили время отклика
- ✅ Убедились, что сертификаты настроены правильно

**НЕ удаляйте, если:**
- ❌ Планируете развёртывание на новых серверах
- ❌ Нужно отлаживать сетевые проблемы
- ❌ Требуется регулярная проверка соединения

---

## 🎯 Быстрое удаление (PowerShell):

```powershell
# Создайте резервную копию
Copy-Item server_user_1/app.py server_user_1/app.py.backup
Copy-Item server_user_1/templates/index.html server_user_1/templates/index.html.backup

# Затем вручную удалите блоки с комментариями "ТЕСТОВЫЙ"
```

---

## ✅ Итог:

Тестовый код TLS полностью изолирован и помечен комментариями. Его удаление не повлияет на:
- Основную логику шифрования
- Восстановление ключей
- Отправку/приём реальных сообщений
- Работу QKD модулей
- Базу данных

**Удаление займёт ~5 минут и не требует изменения логики!**

