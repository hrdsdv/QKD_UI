# 🌐 Настройка сети для работы на двух разных ПК

## Проблема
При попытке обмена сообщениями между двумя ПК возникает ошибка:
```
HTTPConnectionPool(host='localhost', port=5001): Max retries exceeded
```

Это происходит потому, что в конфигурации используется `localhost` или `127.0.0.1`, которые работают только на одном компьютере.

## Решение

### Шаг 1: Узнайте IP-адреса обоих ПК

**На каждом ПК выполните команду:**

**Windows:**
```powershell
ipconfig
```

**Linux/Mac:**
```bash
ip addr
# или
ifconfig
```

Найдите IP-адрес в локальной сети (обычно начинается с `192.168.x.x` или `172.16.x.x`).

**Пример:**
- ПК1 (server_user_1): `172.16.111.52` (порт 5000)
- ПК2 (server_user_2): `172.16.111.53` (порт 5001)

### Шаг 2: Настройте config.py на ПК1 (server_user_1)

Откройте файл `config.py` на **ПК1** и измените:

```python
# Адрес сервера Б (для обращения с сервера А)
REMOTE_SERVER_B = 'http://172.16.111.53:5001'  # ✅ IP ПК2
```

**Уже исправлено!** ✅

### Шаг 3: Настройте config.py на ПК2 (server_user_2)

Откройте файл `config.py` на **ПК2** и измените:

```python
# Адрес сервера А (для обращения с сервера Б)
REMOTE_SERVER_A = 'http://172.16.111.52:5000'  # ✅ IP ПК1 (замените на ваш IP!)
```

**⚠️ ВАЖНО:** Замените `172.16.111.52` на реальный IP-адрес ПК1!

### Шаг 4: Проверьте брандмауэр

Убедитесь, что порты 5000 и 5001 открыты в брандмауэре Windows:

**Windows:**
```powershell
# Разрешить входящие подключения на порт 5000
New-NetFirewallRule -DisplayName "QKD Server 5000" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow

# Разрешить входящие подключения на порт 5001
New-NetFirewallRule -DisplayName "QKD Server 5001" -Direction Inbound -LocalPort 5001 -Protocol TCP -Action Allow
```

Или через графический интерфейс:
1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Создайте правила для портов 5000 и 5001 (входящие подключения)

### Шаг 5: Перезапустите серверы

После изменения `config.py` перезапустите оба сервера:

**ПК1:**
```powershell
cd server_user_1
python app.py
```

**ПК2:**
```powershell
cd server_user_2
python app.py
```

### Шаг 6: Проверьте подключение

**На ПК1** откройте браузер: `http://127.0.0.1:5000`  
**На ПК2** откройте браузер: `http://127.0.0.1:5001`

Попробуйте отправить тестовое сообщение через чат.

## Проверка конфигурации

После настройки проверьте, что в `config.py`:

**На ПК1:**
- ✅ `REMOTE_SERVER_B = 'http://<IP_ПК2>:5001'` (не localhost!)

**На ПК2:**
- ✅ `REMOTE_SERVER_A = 'http://<IP_ПК1>:5000'` (не localhost!)

## Отладка

Если проблема сохраняется:

1. **Проверьте, что серверы запущены:**
   - ПК1: `http://<IP_ПК1>:5000` должен открываться
   - ПК2: `http://<IP_ПК2>:5001` должен открываться

2. **Проверьте ping:**
   ```powershell
   # На ПК1
   ping <IP_ПК2>
   
   # На ПК2
   ping <IP_ПК1>
   ```

3. **Проверьте логи:**
   - Файл `logs/system.log` содержит подробную информацию об ошибках

4. **Проверьте, что используется правильный config.py:**
   - Убедитесь, что файл `config.py` находится в корне проекта
   - Проверьте, что значения в нем соответствуют вашим IP-адресам

## Пример правильной конфигурации

**config.py на ПК1 (172.16.111.52):**
```python
REMOTE_SERVER_B = 'http://172.16.111.53:5001'
```

**config.py на ПК2 (172.16.111.53):**
```python
REMOTE_SERVER_A = 'http://172.16.111.52:5000'
```
