# 🔒 Руководство по тестированию TLS-канала

## 🚀 Быстрый тест

### Шаг 1: Запустите серверы

```powershell
# Терминал 1
cd C:\Users\Vaan\Desktop\qkd_encryption_system\server_user_1
python app.py

# Терминал 2
cd C:\Users\Vaan\Desktop\qkd_encryption_system\server_user_2
python app.py
```

### Шаг 2: Откройте браузеры

- **Сервер А:** http://localhost:5000 → Войти как `UserA` / `password123`
- **Сервер Б:** http://localhost:5001 → Войти как `UserB` / `password123`

### Шаг 3: Откройте окно TLS-теста

На обоих серверах нажмите на **фиолетовую кнопку 🔒 TLS** в правом нижнем углу экрана.

---

## 📝 Тестовые сценарии

### Тест 1: Отправка с сервера А на сервер Б

**На сервере А:**
1. Нажать **🔒 TLS** (правый нижний угол)
2. Проверить статус: `✅ TLS Активен` или `⚠️ TLS Выключен`
3. Ввести текст: `Тестовое сообщение от Абонента А`
4. Нажать **"Отправить тест"**
5. ✅ Увидеть:
   - `✅ Успешно отправлено!`
   - `Время отклика: XX мс`
   - `TLS: Включен ✅` (или `Выключен ⚠️`)

**На сервере Б:**
1. Открыть окно **🔒 TLS**
2. Подождать 3 секунды (автообновление)
3. ✅ В разделе "Полученные тестовые сообщения" увидеть:
   ```
   От: UserA | 27.11.2025, 15:30:00
   Тестовое сообщение от Абонента А
   ```

---

### Тест 2: Отправка с сервера Б на сервер А

**На сервере Б:**
1. Открыть **🔒 TLS**
2. Ввести: `Ответ от Абонента Б`
3. Нажать **"Отправить тест"**
4. ✅ Увидеть подтверждение

**На сервере А:**
1. Проверить полученные сообщения
2. ✅ Увидеть ответ от UserB

---

### Тест 3: Проверка времени отклика

**Цель:** Убедиться, что TLS не замедляет соединение

**Ожидаемое время отклика:**
- **Без TLS (HTTP):** 5-20 мс
- **С TLS (HTTPS):** 10-50 мс

**Если время > 100 мс:**
- Проверьте сетевое соединение
- Проверьте, не блокирует ли брандмауэр
- Проверьте настройки TLS в `config.py`

---

### Тест 4: Проверка с TLS

**Включите TLS в config.py:**

```python
USE_TLS = True
VERIFY_SSL = False  # Для самоподписанных сертификатов
```

**Сгенерируйте сертификаты:**

```powershell
python ssl/generate_certs.py
```

**Перезапустите серверы и повторите Тест 1-3**

**Ожидаемый результат:**
- ✅ Статус: `✅ TLS Активен (HTTPS)`
- ✅ URL: `https://localhost:5000` и `https://localhost:5001`
- ✅ Сообщения передаются успешно

---

## 🐛 Устранение неполадок

### Ошибка: "Connection refused"

**Причина:** Удалённый сервер не запущен

**Решение:**
```powershell
# Проверьте, запущены ли оба сервера
netstat -an | findstr "5000 5001"
```

---

### Ошибка: "SSL: CERTIFICATE_VERIFY_FAILED"

**Причина:** Самоподписанный сертификат не доверен

**Решение:** В `config.py` установите:
```python
VERIFY_SSL = False
```

---

### Ошибка: "Timeout"

**Причина:** Слишком долгий отклик или брандмауэр блокирует

**Решение:**
```powershell
# Разрешите порты в брандмауэре
netsh advfirewall firewall add rule name="QKD Server A" dir=in action=allow protocol=TCP localport=5000
netsh advfirewall firewall add rule name="QKD Server B" dir=in action=allow protocol=TCP localport=5001
```

---

### Сообщения не приходят

**Причина:** Автообновление не работает

**Решение:**
1. Обновите страницу вручную (F5)
2. Проверьте консоль браузера (F12)
3. Нажмите кнопку "Очистить" и попробуйте снова

---

## 📊 Что проверяет тест:

| Параметр | Проверка |
|----------|----------|
| **Соединение** | Может ли сервер А достучаться до сервера Б |
| **TLS** | Включен ли HTTPS протокол |
| **Время отклика** | Скорость передачи данных |
| **Сериализация** | JSON корректно передаётся |
| **Сессии** | Данные сохраняются в сессии |
| **Автообновление** | Сообщения обновляются каждые 3 сек |

---

## ✅ Критерии успешного теста:

- ✅ Сообщение отправляется без ошибок
- ✅ Время отклика < 100 мс
- ✅ Сообщение появляется у получателя через 3 сек
- ✅ Статус TLS отображается корректно
- ✅ Можно отправлять в обе стороны (А→Б и Б→А)

---

## 🎯 После успешного тестирования:

1. **Убедитесь, что основная функциональность работает:**
   - Генерация ключей
   - Восстановление ключей
   - Шифрование/дешифрование сообщений
   - Отправка зашифрованных сообщений

2. **Если всё работает, удалите тестовый код:**
   - См. `TLS_TEST_REMOVAL_GUIDE.md`
   - Удаление займёт 5-10 минут
   - Не влияет на основную логику

3. **Если есть проблемы:**
   - Оставьте тестовый код для отладки
   - Используйте его для диагностики сетевых проблем
   - Проверьте настройки TLS в `config.py`

---

## 💡 Полезные команды:

```powershell
# Проверка открытых портов
netstat -an | findstr "5000 5001"

# Проверка сертификатов
dir ssl\*.pem

# Просмотр логов
Get-Content qkd_system.log -Tail 50

# Очистка логов
Clear-Content qkd_system.log
```

---

## 🎉 Готово!

Тестовый интерфейс TLS полностью функционален и готов к использованию!

